# Datasets

This directory contains datasets or links to datasets used in AI/ML projects.

## Important
- For large datasets, store links or download scripts rather than the data itself
- Include dataset documentation (format, source, license)
- Add preprocessing scripts if applicable

## Organization
- Each dataset should have its own subdirectory
- Include a README describing the dataset
- Store metadata and data dictionaries
